{
  "name": "secure-rehab-docs",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix web",
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix web\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
